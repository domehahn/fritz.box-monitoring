global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - 'rules/*.yml'

scrape_configs:
  - job_name: 'fritz'
    scrape_interval: 30s
    scrape_timeout: 10s
    static_configs:
      - targets: ['fritz-exporter:8000']
        labels:
          instance_type: 'fritz_exporter'
